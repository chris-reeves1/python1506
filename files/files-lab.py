import csv

companies = []
sales = []

with open("output.csv") as csvfile:
    reader = csv.reader(csvfile)
    next(reader)
    for loop:
        append companies top companies
        append sales to sales: cast to int, remove commas(replace(",", ""))

monthly_sum = [expression-sum(x) item for x in iterable zip(*sales)] 

yearly_sum {} = 
for loop for companies
    yearlysum[i] = sum(sales[i])

print
for loo .items()